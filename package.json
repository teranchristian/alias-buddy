{
  "name": "alias-buddy",
  "version": "1.3.2",
  "private": true,
  "description": "Local-first, cross-browser aliases for native people search in web applications.",
  "scripts": {
    "dev": "wxt -b chrome --mv3",
    "dev:edge": "wxt -b edge --mv3",
    "dev:firefox": "wxt -b firefox --mv3",
    "test": "node --test",
    "build": "npm run build:chrome && npm run build:edge && npm run build:firefox",
    "build:chrome": "wxt build -b chrome --mv3",
    "build:edge": "wxt build -b edge --mv3",
    "build:firefox": "wxt build -b firefox --mv3",
    "validate": "node scripts/validate-extension.mjs",
    "check": "npm run test && npm run build && npm run validate",
    "package": "bash scripts/package-extension.sh"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "wxt": "0.21.4"
  }
}
