{
  "name": "alias-buddy",
  "version": "1.2.1",
  "private": true,
  "description": "Local-first aliases for native people search in web applications.",
  "scripts": {
    "test": "node --test",
    "validate": "node scripts/validate-extension.mjs",
    "check": "npm run test && npm run validate",
    "package": "bash scripts/package-extension.sh"
  },
  "engines": {
    "node": ">=20"
  }
}
